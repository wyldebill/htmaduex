rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP admin policy: any authenticated user can manage business records.
    match /businesses/{businessId} {
      allow read, write: if request.auth != null;
    }
  }
}
