{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); if [[ \"$f\" == *.dart ]]; then cd /Users/williamaustin/Projects/flutter/htmaduex && flutter analyze && flutter test; fi",
            "timeout": 300,
            "statusMessage": "Running flutter analyze & flutter test..."
          }
        ]
      }
    ]
  }
}
