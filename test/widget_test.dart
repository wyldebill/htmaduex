import 'package:flutter_test/flutter_test.dart';

void main() {
  test('basic smoke test', () {
    // Keep a tiny unit test so CI still validates the Dart test pipeline.
    expect(2 + 2, 4);
  });
}
